extends ../../src/home/manual-template

block body
    div.everything

block script
    script.
        require([
            'test/file-tree/manual',
            'backbone',
            'backbone-relational'
        ], function (index) {
            index.init()
        })